services:
  - type: web
    name: your-service-name
    env: python
    buildCommand: "pip install -r requirements.txt && apt-get update && apt-get install -y chromium"
    startCommand: "python app.py"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: your-db-name
          property: connectionString
      - key: TELEGRAM_TOKEN
        value: "7846698102:AAFR2bhmjAkPiV-PjtnFIu_oRnzxYPP1xVo"
      - key: RENDER_EXTERNAL_HOSTNAME
        sync: false